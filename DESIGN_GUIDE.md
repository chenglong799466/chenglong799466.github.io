# 设计规范指南

## 🎨 配色系统

### 主色调
```css
--primary-color: #165DFF;    /* 低饱和蓝色 - 主要操作、链接、按钮 */
```

### 背景色
```css
--bg-color: #FFFFFF;         /* 纯白 - 主背景 */
--bg-secondary: #F5F7FA;     /* 浅灰 - 次要背景、卡片背景 */
```

### 文字色
```css
--text-primary: #333333;     /* 深灰 - 标题、正文 */
--text-secondary: #666666;   /* 浅灰 - 辅助文字、说明 */
```

### 边框色
```css
--border-color: #E5E6EB;     /* 浅灰 - 分割线、边框 */
```

### 使用场景

| 颜色 | 使用场景 | 示例 |
|------|---------|------|
| `#165DFF` | 主要按钮、链接、标签、导航高亮 | 提交按钮、文章标签 |
| `#FFFFFF` | 页面背景、卡片背景 | 主内容区、文章卡片 |
| `#F5F7FA` | 次要背景、代码行内背景 | 侧边栏背景、`code` 标签 |
| `#333333` | 标题、正文 | H1-H6、段落文字 |
| `#666666` | 辅助文字 | 日期、作者、说明文字 |
| `#E5E6EB` | 分割线、边框 | 卡片边框、分隔线 |

## 📐 排版系统

### 字体家族
```css
font-family: 'Inter', 'Noto Sans SC', -apple-system, BlinkMacSystemFont, sans-serif;
```

- **Inter**：英文字体，现代、清晰
- **Noto Sans SC**：中文字体（思源黑体），专业、易读

### 字号层级

| 元素 | 字号 | 字重 | 使用场景 |
|------|------|------|---------|
| H1 | 32px | 700 | 页面主标题 |
| H2 | 24px | 600 | 章节标题 |
| H3 | 20px | 600 | 小节标题 |
| H4 | 18px | 600 | 子标题 |
| 正文 | 16px | 400 | 正文内容 |
| 小字 | 14px | 400-500 | 辅助信息、标签 |
| 微字 | 12px | 400 | 次要信息 |

### 行高规范
```css
line-height: 1.7-1.8;  /* 正文 */
line-height: 1.4-1.5;  /* 标题 */
line-height: 1.6;      /* 代码块 */
```

## 📏 间距系统

### 基准间距：8px

| 倍数 | 像素值 | 使用场景 |
|------|--------|---------|
| 1x | 8px | 最小间距、图标间距 |
| 1.5x | 12px | 小元素间距、标签间距 |
| 2x | 16px | 常规间距、段落间距 |
| 2.5x | 20px | 卡片内边距 |
| 3x | 24px | 区块间距 |
| 4x | 32px | 大区块间距 |
| 5x | 40px | 章节间距 |
| 6x | 48px | 页面区域间距 |

### 内边距（Padding）
```css
/* 卡片 */
padding: 28px;          /* 桌面端 */
padding: 20px;          /* 移动端 */

/* 按钮 */
padding: 8px 16px;      /* 小按钮 */
padding: 12px 32px;     /* 大按钮 */

/* 标签 */
padding: 4px 12px;      /* 标签 */
```

### 外边距（Margin）
```css
/* 标题 */
margin-bottom: 16px;    /* H2-H4 */
margin-top: 32px;       /* H2 */

/* 段落 */
margin-bottom: 16px;    /* 段落 */

/* 区块 */
margin-bottom: 24px;    /* 小区块 */
margin-bottom: 40px;    /* 大区块 */
```

## 🔲 圆角系统

| 元素 | 圆角值 | 使用场景 |
|------|--------|---------|
| 4px | 小圆角 | 按钮、标签、代码块 |
| 8px | 中圆角 | 卡片、输入框 |
| 12px | 大圆角 | 大卡片 |
| 50% | 圆形 | 头像、圆形按钮 |

```css
border-radius: 4px;     /* 按钮、标签 */
border-radius: 8px;     /* 卡片 */
border-radius: 50%;     /* 头像 */
```

## 🌑 阴影系统

### 三级阴影

```css
/* 小阴影 - 轻微悬浮 */
--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.04);

/* 中阴影 - 明显悬浮 */
--shadow-md: 0 4px 12px rgba(0, 0, 0, 0.08);

/* 大阴影 - 强调悬浮 */
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.12);
```

### 使用场景

| 阴影 | 使用场景 |
|------|---------|
| `shadow-sm` | 导航栏、默认卡片 |
| `shadow-md` | Hover 状态的卡片 |
| `shadow-lg` | 模态框、弹出层 |

## 🎭 交互效果

### 过渡动画
```css
transition: all 0.2s ease;  /* 标准过渡 */
```

### Hover 效果

#### 卡片 Hover
```css
.card:hover {
    border-color: var(--primary-color);
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
}
```

#### 按钮 Hover
```css
.button:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}
```

#### 链接 Hover
```css
.link:hover {
    color: var(--primary-color);
}
```

#### 标签 Hover
```css
.tag:hover {
    background: var(--primary-color);
    color: white;
    transform: translateY(-1px);
}
```

## 📱 响应式断点

### 断点设置
```css
/* 移动端 */
@media (max-width: 768px) { }

/* 平板端 */
@media (min-width: 769px) and (max-width: 1023px) { }

/* 桌面端 */
@media (min-width: 1024px) { }
```

### 布局变化

| 屏幕尺寸 | 布局 | 说明 |
|---------|------|------|
| ≥1024px | 三栏 | 导航 + 侧边栏 + 内容 |
| 768-1023px | 两栏 | 导航 + 内容（侧边栏移至顶部） |
| <768px | 单栏 | 垂直堆叠 |

## 🎯 组件规范

### 按钮
```css
/* 主要按钮 */
.btn-primary {
    background: var(--primary-color);
    color: white;
    padding: 12px 32px;
    border-radius: 4px;
    font-weight: 500;
}

/* 次要按钮 */
.btn-secondary {
    background: var(--bg-secondary);
    color: var(--text-primary);
    border: 1px solid var(--border-color);
}
```

### 卡片
```css
.card {
    background: var(--bg-color);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    padding: 28px;
    transition: all 0.2s ease;
}
```

### 标签
```css
.tag {
    display: inline-block;
    padding: 4px 12px;
    background: var(--bg-secondary);
    color: var(--text-secondary);
    border-radius: 4px;
    font-size: 13px;
}
```

## ✍️ 内容规范

### 代码块
```css
/* 行内代码 */
code {
    background: var(--bg-secondary);
    padding: 3px 8px;
    border-radius: 4px;
    color: var(--primary-color);
    font-size: 0.9em;
}

/* 代码块 */
pre {
    background: #1E1E1E;
    padding: 20px;
    border-radius: 8px;
    overflow-x: auto;
}
```

### 引用块
```css
blockquote {
    border-left: 4px solid var(--primary-color);
    padding-left: 20px;
    background: var(--bg-secondary);
    padding: 16px 20px;
    border-radius: 0 4px 4px 0;
}
```

### 表格
```css
table {
    width: 100%;
    border-collapse: collapse;
}

th, td {
    border: 1px solid var(--border-color);
    padding: 12px;
}

th {
    background: var(--bg-secondary);
    font-weight: 600;
}
```

## 🎨 设计原则

### 1. 极简主义
- 删除一切不必要的元素
- 保持页面清爽、留白充足
- 避免过度装饰

### 2. 一致性
- 统一的配色方案
- 统一的间距系统
- 统一的圆角规范

### 3. 可读性
- 清晰的字号层级
- 舒适的行高设置
- 合理的颜色对比

### 4. 响应式
- 适配所有设备
- 优化触摸交互
- 保持体验一致

### 5. 性能
- 最小化 CSS
- 优化动画性能
- 快速加载

## 📋 检查清单

在发布前，请检查：

- [ ] 配色符合规范（不超过4种主色调）
- [ ] 字号层级清晰
- [ ] 间距使用8px基准
- [ ] 圆角统一（4px/8px）
- [ ] 阴影使用规范
- [ ] Hover 效果流畅
- [ ] 响应式布局正常
- [ ] 移动端体验良好

---

**遵循这些规范，保持博客的专业性和一致性！** ✨
