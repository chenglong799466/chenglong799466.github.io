---
layout:     post
title:      Git的使用场景
subtitle:   Git Usage Scenarios and Common Commands
date:       2024-01-11
author:     chenglong
description: Git常用命令和实际使用场景总结，包括.gitignore文件配置、新建项目流程、远程仓库管理等实用场景
categories: [版本控制, 开发工具]
header-img: img/post-bg-coffee.jpeg
catalog: true
tags:
    - Git
    - 版本控制
    - 命令行
    - GitHub
    - 开发工具
---

# Git添加gitignore文件

`git rm -r --cached .`  #清除缓存
`git add .` #重新trace file
`git commit -m "update .gitignore"` #提交和注释
`git push origin master` #可选，如果需要同步到remote上的话

# Git新建项目并推送远端github

`echo "# test" >> README.md`
`git init`
`git add .`
`git commit -m "First commit"`
`git branch -M main`
`git remote add origin git@github.com:chenglong799466/test.git` # 修改为你的git项目地址
`git push -u origin main`